.env
__pycache__/
*.pyc
.venv/
venv/

# 수집 원문은 평문이므로 커밋 금지. 배포는 암호화된 web/data/*.enc 로만 나간다.
# data/ 전체를 제외한다 — state.json 같은 실행 상태 파일도 레포에 남길 이유가 없다.
data/
*.session

# 배포 번들은 GitHub Actions 가 매 실행마다 새로 만들어 Pages 아티팩트로 올린다.
# 레포에는 커밋하지 않는다 — git 히스토리가 매일 수 MB 씩 불어나는 걸 막기 위함.
web/data/
