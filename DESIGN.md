# 대상 채널 요약 + RAG Q&A — 설계

## 1. 무엇을 만드는가

비공개 텔레그램 채널 **"주식 채널"** 의 글을 매일 수집해서

1. **관점별 일일 요약** — 국장 / 미장으로 나누고, 그가 실제로 쓰는 사고 틀(관점) 8가지로 분해
2. **RAG 질의응답** — 수집한 원문 전체에 대해 자연어로 질문

를 제공하는 정적 웹앱. 친한 몇 명만 공유 암호로 접근.

## 2. 원본 데이터 성격 (실측)

2026-05-06 ~ 06-26, 28일간 **3,413건** 분석 결과.

| 항목 | 값 |
|---|---|
| 하루 평균 | 약 200건 |
| 사진 포함 | 64% (613/959) — 캡션 없는 사진도 많음 |
| 평균 글자수 | 메시지당 약 135자 (짧고 파편적) |
| 성격 | 장문 리포트가 아니라 **실시간 중계에 가까운 짧은 코멘트 스트림** |

→ 그래서 "글 하나를 요약"이 아니라 **하루치 스트림을 관점별로 재구성**하는 게 맞음.

### 하루 운영 리듬 (시간대별 메시지 수, 28일 누적)

```
05-07시  724   미장 마감 정리 · 미국 RS/소라티노 · 당일 전략 브리핑
08-09시  652   국장 장전 · 신고가 · 목표가 조정
10-15시  817   국장 장중 실시간 매매 (비중 조절이 여기 집중)
16-19시  677   국장 마감 정리 · 수급 분석 · 투자 원칙
22-23시  440   미장 장중 대응
```

## 3. 그의 고유 지표 어휘 (요약·RAG가 반드시 알아야 함)

| 용어 | 의미 |
|---|---|
| **피어앤그리드 오실레이터** | 자체 Fear&Greed 지표. 국장/미장 별도. `과열 / 중립 / 과매도 / 바닥권` 구간으로 말함 |
| **소라티노 지표** | Sortino 기반 섹터 강도. "10일/10주 이평선 이상 생존 섹터"로 언급 |
| **업종쏠림지수** | 올라가면 시장 나쁨, 내려와야 시장 좋아짐 |
| **거래대금 오실레이터** | 섹터별 자금 쏠림 |
| **RS 70 이상** | Mansfield RS. 주도업종 판정 기준선 |
| **수급 빈집** | 수급 오실레이터상 매물 소진 종목군 |
| **임펄스 신호** | 초록(매수) / 파랑(중립) / 빨강(매도) |
| **매도 시그널** | **"신고가 갱신 후 음전"** 또는 **"10일선 이탈"** ← 그의 2대 매도 트리거 |
| **시총대비 / 금액대비** | 사모·투신·연금 수급을 두 기준으로 랭킹 |
| **닉스 / 닉전 / 스퀘어** | 하이닉스 / 하이닉스+삼성전자 / SK스퀘어 |
| **현금비중** | 모든 판단의 최종 출력. %로 명시함 |

## 4. 관점(View) 8종 — 요약의 뼈대

실제 메시지에서 반복 관찰되는 사고 틀. 국장/미장 각각에 대해 채움.

| # | id | 라벨 | 무엇을 담나 |
|---|---|---|---|
| 1 | `regime` | 시장 국면 | 지수 위치, 3/5/10일 이평선, 피어앤그리드 오실레이터 구간, 임펄스 |
| 2 | `cash` | **현금비중·포지션** | 오늘 현금 몇%→몇%, 왜, 다음 트리거. ★그의 시그니처 |
| 3 | `leaders` | 주도업종 | RS 70+, 소라티노 생존 섹터, 52주/250일 신고가, 이격도 |
| 4 | `flow` | 수급 | 사모/투신/연금 × 시총대비·금액대비 섹터 랭킹, 빈집, 거래대금 (주로 국장) |
| 5 | `trades` | 매매 액션 | 종목명 + 비중 변화 + 사유 |
| 6 | `signals` | 시그널·룰 발동 | 신고가후 음전 / 10일선 이탈 / 과매도 / MDD 단계 |
| 7 | `macro` | 거시·유동성 | 채권금리, 달러인덱스, 환율, TGA, 정책·세제 |
| 8 | `principles` | 원칙·멘탈 | 교훈성 메시지 ("기준을 스스로 마련해라" 류) |

부가: `news` (뉴스·리포트 공유), `etc` (잡담·공지) — 분류는 하되 요약 카드에서는 접어둠.

### 최상단 헤드라인

관점 요약 위에 항상 이 3개를 뽑음:

- **한 줄 결론** — 오늘 그의 스탠스
- **현금비중 변화** — `국장 15% → 56%` 처럼 숫자로 (하루 중 최초/최종 언급 추출)
- **오늘 바뀐 것** — 어제 대비 스탠스 변화

## 5. 파이프라인

```
[1] crawl      Telethon → 지정 기간(1~4주) 메시지 + 사진 다운로드
      ↓
[2] vision     캡션 없는 사진 → Gemini 비전 → 표/차트 내용 텍스트화   ※ 현재 비활성
      ↓
[3] classify   메시지 → (국장|미장|공통) × 관점 8종 분류
      ↓
[4] summarize  하루치 × 관점별 요약 + 헤드라인 + 현금비중 추출
      ↓
[5] embed      메시지를 시간 블록으로 청킹 → 임베딩 (256차원 int8 양자화)
      ↓
[6] bundle     요약 + 청크 + 임베딩 → AES-GCM 암호화 → web/data/*.enc
      ↓
[7] deploy     GitHub Pages (Actions 아티팩트, 레포에 커밋하지 않음)
```

### 모델 — 쿼터가 결정했다

무료 등급 한도를 실호출로 확인한 결과가 선택을 강제했다 (2026-08 실측):

| 모델 | 실측 결과 |
|---|---|
| `gemini-2.5-flash` | **404** — 신규 사용자에게 제공 중단 |
| `gemini-3.6-flash` | **하루 20회** — 표 판독 정확도는 최고지만 파이프라인엔 쓸 수 없음 |
| `gemini-3.5-flash` | 여유 있음 |
| `gemini-3.5-flash-lite` | 가장 넉넉 |
| `gemini-2.0-flash` | 분당 한도 즉시 |

→ **전 단계를 `gemini-3.5-flash-lite` 로 통일**했다. 매일 빠짐없이 도는 것이
요약문 몇 줄의 품질보다 중요하기 때문이다. 임베딩만 `gemini-embedding-001`(256차원).

`flash` 와 `flash-lite` 를 같은 날짜로 A/B 비교했을 때 현금비중 숫자와 사실관계는
동일했고, 차이는 요약문 길이(2,217자 → 1,296자)뿐이었다. 다만 `flash-lite` 는
근거 메시지 id(`refs`)를 채우지 않는 성향이 있어 프롬프트에서 필수로 못 박아야 한다.

### 임베딩 배치의 함정

`batchEmbedContents` 는 HTTP 로는 1회지만 **쿼터는 배치 안의 텍스트 수만큼** 깎인다.
호출 횟수만 세는 속도 제한으로는 50개 배치에서 즉시 429 가 난다.
`llm.py` 의 속도 제한이 `weight` 로 배치 크기를 받는 이유다.

## 6. 배포 / 접근 제어

```
GitHub Actions (매일 00:10 KST + 수동 버튼)
    └→ 크롤 → 요약 → 임베딩 → bundle.enc 커밋
GitHub Pages (public repo, 무료)
    └→ 첫 방문 시 공유 암호 입력 → AES-GCM 복호화 → 화면
버튼 클릭 → workflow_dispatch API → 재크롤
```

- 데이터는 **AES-256-GCM**으로 암호화해서 커밋. 암호 없이는 원문·요약 모두 못 읽음.
- 키 유도: PBKDF2-SHA256, 600k iterations.
- `GEMINI_API_KEY`(무료등급)와 워크플로 트리거용 fine-grained PAT도 같은 번들 안에 암호화.
- 레포는 public이지만 **평문 콘텐츠가 0** 이므로 검색엔진에 채널 내용이 노출되지 않음.

## 7. 이미지 정책 — 판독은 현재 꺼져 있다

원래 이미지 판독이 이 프로젝트의 핵심 설계였다. 메시지의 64%가 사진이고
수급 순위표·차트가 대부분이라, 사진을 버리면 요약의 절반이 날아가기 때문이다.

판독 자체는 **검증을 마쳤다** — `gemini-3.6-flash` 로 50행 수급 순위표를
종목명·시가총액·수익률·순위까지 정확히 읽었다. 한국 증시 표는 파란색이 음수인데
그 부호를 빠뜨리는 문제도 프롬프트로 잡았다.

**그런데 쿼터가 막았다.** `gemini-3.6-flash` 무료 등급이 하루 20회뿐인데,
일주일치 사진 501장을 판독하려면 시간대별로 묶어도 139회가 필요하다.
(낱장 사진이 3분의 1이라 묶기 전에는 291회였다.)

→ `config.yaml` 의 `vision.enabled: false`. 현재 동작:

- 사진은 계속 내려받는다 (800px webp, 최근 4주 보관)
- 요약이 근거로 인용한 사진은 화면에 썸네일로 보인다 (번들과 같은 키로 암호화)
- **캡션 없는 사진의 내용은 요약·질의응답에 들어가지 않는다** ← 실질적 손실
- `true` 로 바꾸면 이미 받아둔 사진부터 이어서 판독한다. 재크롤 불필요

되살리려면 유료 등급 키를 쓰거나, 판독 대상을 좁히면 된다
(예: 수급 분석 시간대인 18~19시 사진만).

## 8. 채널 교체

운영자가 **6개월마다 채널을 새로 개설**함 (직전 채널은 2026-06-26 종료).
→ 채널 링크는 `config.yaml` 의 `telegram.channels` 배열로 관리. 과거 채널 데이터는 그대로 두고 새 링크만 추가하면 이어서 수집됨.
