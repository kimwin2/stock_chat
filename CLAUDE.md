# stock_chat — 프로젝트 가이드 (AI 에이전트용)

## 무엇인가

비공개 텔레그램 채널 "주식 채널"을 매일 수집해서
**관점별 일일 요약**과 **RAG 질의응답**을 보여주는 정적 웹앱.
친한 몇 명만 공유 암호로 접근한다. 설계 근거는 [DESIGN.md](DESIGN.md).

**스택**: Python 3.12 (빌드 도구 없음) + vanilla JS · Gemini REST 직접 호출 ·
GitHub Actions(자정 cron) + GitHub Pages · AES-256-GCM 클라이언트 복호화

## 스킬

| Skill | 언제 |
|---|---|
| `/pipeline-ops` | 파이프라인이 안 돌 때, 쿼터·세션·채널 링크 문제, Actions 실패 |
| `/summary-tune` | 요약·질의응답 품질 손볼 때, 관점 추가/변경, 검색이 엉뚱할 때 |

## 이 프로젝트의 핵심 사실

- **채널이 6개월마다 바뀐다.** 직전 채널은 2026-06-26 종료. `config.yaml` 의
  `telegram.channels` 배열로 관리하고, 닫힌 채널 항목은 지우지 않는다.
- **메시지의 64%가 사진**이고 캡션 없는 사진이 많다. 원래 이미지 판독(`vision.py`)이
  요약 품질의 핵심이었지만, **무료 쿼터로는 감당이 안 돼 현재 꺼져 있다**
  (`config.yaml` 의 `vision.enabled: false`). 켜면 받아둔 사진부터 이어서 판독한다.
  판독 자체는 검증돼 있다 — 50행 수급표를 숫자·부호까지 정확히 읽는다.
- **하루 약 200건**의 짧은 코멘트 스트림이다. 글 하나를 요약하는 게 아니라
  하루치를 관점별로 재구성하는 구조다.
- **모든 판단이 "현금비중 %"로 수렴한다.** 요약에서 숫자를 뭉개면 가치가 사라진다.
- `pipeline/views.py` 가 설계의 중심. 관점 8종 + 그의 고유 용어 사전(`GLOSSARY`)이
  분류·요약·질의응답 프롬프트에 전부 주입된다.

## 코드 규칙

### Python (`pipeline/`)

- 각 단계는 **이어달리기**다. 이미 처리한 것은 건너뛰고, 쿼터가 소진되면 거기까지
  저장하고 정상 종료한다. 새 단계를 추가할 때도 이 성질을 지킬 것.
- Gemini 는 SDK 대신 `llm.py` 의 REST 래퍼를 쓴다. 속도 제한·백오프·쿼터 감지가 여기 있다.
- 모델 호출 결과는 항상 원본 JSON 에 되쓴다(`data/raw/*.json`). 재실행 시 재사용된다.
- LLM 출력은 믿지 말고 `_clean()` 류 함수로 검증한다. 특히 메시지 id 는 자주 지어낸다.

### 프론트 (`web/`)

- vanilla JS ESM, 빌드 도구 없음. 외부 라이브러리 추가 금지 (CSP 없이도 자립).
- 모든 사용자 표시 문자열은 `esc()` 로 이스케이프. 내용이 LLM·텔레그램에서 온다.
- 라이트/다크 양쪽을 지원한다. 색은 `style.css` 상단 CSS 변수로만 쓰고 하드코딩 금지.
- 그리드 안에 가로 스크롤 요소를 넣을 땐 `min-width: 0` 을 빠뜨리지 말 것
  (날짜 스트립 때문에 페이지 전체가 가로로 밀린 적 있음).

### 비밀

- `.env` 는 커밋 안 됨. 신규 키는 `.env.example` 도 같이 갱신.
- **`data/` 와 `web/data/` 를 커밋하지 말 것.** 원문은 평문이고, 배포 번들은
  Actions 가 매번 새로 만들어 Pages 아티팩트로만 올린다 (git 히스토리 비대 방지).
- 레포는 public 이지만 배포물에 평문 콘텐츠가 0 이어야 한다. 이 불변식을 깨지 말 것.

## 실행 명령

```bash
python -m pipeline.run                   # 전체 (최근 1주)
python -m pipeline.run --weeks 4
python -m pipeline.run --only summarize --force-summary
python -m pipeline.tg_auth               # 세션 발급
python -m tools.import_legacy            # 예전 채널 덤프 가져오기
python -m tools.mock_summaries           # 키 없이 UI 확인용

cd web && python -m http.server 8811 --bind 127.0.0.1
```

## 검증 자산

- `data/raw/` 에 2026-05-06 ~ 06-26 실제 메시지 **3,413건**이 들어있다 (예전 채널).
  프롬프트나 청킹을 고쳤을 때 여기로 바로 확인할 수 있다.
- **2026-06-23** 이 가장 좋은 테스트 날짜다 — 코스피 10일선 이탈로 현금비중을
  15% → 56% 로 올린 날이라 관점 8개가 전부 채워진다.
- Playwright + Chromium 설치돼 있음. UI 변경 후 모바일 390px 가로 스크롤 확인 필수.

## 알려진 제약

- **모델 선택은 쿼터가 결정했다** (2026-08 실측): `gemini-2.5-flash` 는 404,
  `gemini-3.6-flash` 는 하루 20회뿐. `gemini-3.5-flash`(요약·답변)와
  `-flash-lite`(분류)만 파이프라인에 쓸 수 있다. 모델을 바꾸려면 반드시
  실호출로 일일 한도부터 확인할 것 — 문서에 적힌 값과 다르다.
- 일일 한도에 걸리면 그 지점까지 저장하고 정상 종료한다. 며칠에 나눠 채워지는 게 정상.
- GitHub Actions 는 데이터센터 IP 에서 텔레그램에 접속한다. 세션이 끊기면 재발급 필요.
- 이미지 원본은 28일만 보관(`crawl.media_retention_days`). 판독 텍스트는 영구 보관.
